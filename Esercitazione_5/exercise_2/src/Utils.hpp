#ifndef UTILS_HPP
#define UTILS_HPP

#include <iostream>
#include <fstream>
#include <sstream>
#include <vector>
#include "include.hpp" // Include the struct definitions

std::vector<Cell0D> loadCell0DData(const std::string& filePath);

#endif // UTILS_HPP